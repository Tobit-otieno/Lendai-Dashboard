import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MobileRouting = () => {
  return (
    <View>
      <Text>MobileRouting</Text>
    </View>
  );
};

export default MobileRouting;

const styles = StyleSheet.create({});
